# Shared Workspace - Work History

This document logs the work done on the `shared` repository.

## Session: 2026-02-16 (Reorganization)

### Tasks Completed
1.  **Renamed `my-app` to `0190myapp`**:
    -   Renamed the folder inside `shared` to `0190myapp`.
    -   This folder is dedicated to work originating from PC `0190`.

2.  **Organization Rules**:
    -   `0190myapp/`: Work from PC 0190.
    -   `shared/`: Work from other PCs (or specific subfolders therein).

### Next Steps
-   [ ] Verify the rename syncs to other devices.

### Instructions for AI Assistant (on new device)
> "Review `shared/docs/work_history.md`.
> The project `0190myapp` is located at `shared/0190myapp`.
> Continue work on `shared/0190myapp/重量計算表`."
