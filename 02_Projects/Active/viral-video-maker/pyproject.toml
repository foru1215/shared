[project]
name = "viral-video-maker"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "shared-lib",
]

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project.scripts]
viral-video = "src.batch_producer:main"
